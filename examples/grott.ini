# Specify grott monitor configuration
# Defaults are described
# Remove # and update the value to enable the setting
# Last updated: 2024-12-04
# Version 3.0.0

[Generic]
# Specify loglevel for extended messaging, uses standard python logging level: DEBUG,INFO,WARNING,ERROR,CRITICAL added DEBUGV (debug verbose+)
#loglevel = INFO

# Specify verbose for extended messaging (deprecated >3.0.0 use loglevel instead, verbose = True will be defaulted to loglevel = debug!)
#verbose = True

#specify lower minrecl (e.g. minrecl = 1) to log / debug all records. minrecl = 100 will supress most of commincation records except data related records
#minrecl = 100

# Specify mode (sniff/proxy/server/serversa)(> 2.1.0 proxy is default)
#mode = proxy

# Specify port and IP address to listen to (only proxy), default port 5279, 0.0.0.0 ==> own ip address
#ip = 0.0.0.0
#port = 5279

# Only for mode = proxy!!!
#To blocks commands from outside (to channge inverter and shine devices settings) specify blockcmd = True,
# specify noipf = True if you still want be able to dest ip addres from growatt server
# Specify noipf = True if you still want be able to dest ip addres from growatt server (advice only to use # this for a short time)
#blockcmd = True
#noipf = True

# Time = auto/server parameter enable/disable date/time retrieval from data record (server), default is
# auto: grott decides which time is used (data record if valid otherwise Server)
# If time = server Grott server time is alwas used
#time = auto

# Sendbuf = True / False parameter to enable  / disable sending historical (buffered) data. Default is sendbuf = True.
#sendbuf = True

# Specify inverter id (not necessary in version >2.1.0)
#inverterid = ABC1234567

#specify invertype:  default (use standard tl-s type of inverters, auto (>3.0.0, lets grott automatically detect),spf, sph, mod etc use specific invertypes)
#be aware: auto does not detect spf type inverters!!!
#invtype = auto

 #define invertype setting for multiple servers
 # invtypemap = {"invertid" : "auto", "invertid1" : "spf", }

[Growatt]
# Server name/IP address and port of Growatt server
# specify only if the IP address of server.growatt.com is changed
# The address as of Nov 2022 is 47.91.67.66
# Default:                       ip = "server.growatt.com"
# For China:                     server-cn.growatt.com
# For US:                        server-us.growatt.com
# For Australia and New Zealand: server-au.growatt.com

#ip = server.growatt.com
#ip = 47.91.67.66
#port = 5279

[Server]
#In server mode, send data also to growatt servers (one way only, data from growatt will not be processed to block commands comming from growatt!)
#serverpassthrough = True

##
#use settings below only for debug or advanced tuning!!!!!
##
## Define ip/port if grottserver is running standalone and/or at other system
#serverip = 0.0.0.0
#serverport = 5781
#httpip = 0.0.0.0
#httpport = 5782
##Time to sleep waiting on API response
#serverrespwait = 0.5
##Total time in seconds to wait on Iverter Response
#serverinverterrespwait = 10
##Totaal time in seconds to wait on Datalogger Response
#serverloggerrespwait = 5

[MQTT]
# Mqtt parameters definitions
# Be aware nomqtt = True means no MQTT processing will be done!!!!!!

#nomqtt = False
#ip = localhost
#port = 1883
#topic= energy/growatt
#auth = False
#user = grott
#password = growatt2020

[PVOutput]
# PVOutput parameters definitions

#pvoutput = True
#apikey = yourapikey
# Data upload limit (in minutes)
#pvuplimit = 5
# Use this if you have one inverter
#systemid = 12345

# Use this if you have multiple inverters
#pvinverters = 2
#systemid1 = 12345
#inverterid1 = inverter1
#systemid2 = 67890
#inverterid2 = inverter2

#systemid99 = 99999
#inverterid99 = inverter99

[influx]
# Influxdb parameters definitions

#influx = False
#influx2 = False
#dbname = grottdb
#ip = localhost
#port = 8086
#user = grott
#password = growatt2020
#token  = "influx_token"
#org  = "grottorg"
#bucket = "grottdb"

[extension]
# grott extension parameters definitions

#extension = True
#extname = grottext
#extvar = {"var1": "var1_content", "var2": "var2_content"}